import 'package:flutter/material.dart';

class OriginalCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(),
        body: Text("カレンダーが表示される予定です")
    );
  }
}
